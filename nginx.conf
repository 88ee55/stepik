server {
    listen 80;
    server_name *;
    #root /home/box/web;
    location / {
      return 404;
    }
    location /uploads {
      root /home/box/web/uploads;
    }
    location ~* ^.+\.\w+\ {
      root /home/box/web/public;
    }
}
